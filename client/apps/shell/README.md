# shell (host)

Порт dev: **4000**. Module Federation host: layout, React Router, lazy remotes.

- `mfe-auth` — `/login`, `/register`
- `mfe-orders` — `/orders`, `/orders/:id`

См. [docs/microfrontends.md](../../docs/microfrontends.md).
