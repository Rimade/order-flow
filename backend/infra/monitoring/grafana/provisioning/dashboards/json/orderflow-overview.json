{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" } },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "HTTP request rate",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(http_requests_total[1m])) by (service)",
          "legendFormat": "{{service}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" } },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "HTTP latency p95",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le, service))",
          "legendFormat": "{{service}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "title": "Orders created / status transitions",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(orders_created_total[1m]))",
          "legendFormat": "created"
        },
        {
          "expr": "sum(rate(orders_status_transitions_total[1m])) by (status)",
          "legendFormat": "{{status}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "title": "Inventory / payments",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(inventory_reservations_total[1m])) by (result)",
          "legendFormat": "inventory {{result}}"
        },
        {
          "expr": "sum(rate(payments_total[1m])) by (result)",
          "legendFormat": "payment {{result}}"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["orderflow"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "title": "OrderFlow Overview",
  "uid": "orderflow-overview",
  "version": 2
}
